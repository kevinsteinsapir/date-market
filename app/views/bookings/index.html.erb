<div class="container my-3">
  <h2>Booking Index</h2>

  <ul>
  <% @bookings.each do |booking| %>
    <li><%= link_to booking.fecha, booking_path(booking) %></li>
  <% end %>
  </ul>

  <%= link_to "Back to Home", root_path %>
</div>
